import 'package:flutter/material.dart';

class PostAddScreen extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("I am postAdd"),
    );
  }
}